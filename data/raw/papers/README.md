# Research papers (open access)

PDFs are downloaded automatically:

```bash
python scripts/download_papers.py
```

PDF files are gitignored; only this README and the downloader script are in the repository.
